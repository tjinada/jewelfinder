// The "notify when available" watch feature was removed in favour of the booking
// system. This file is intentionally empty (kept only because it can't be deleted
// from here). Safe to delete from the repo.
export {};
