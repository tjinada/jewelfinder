// REMOVED — the email-invite flow was replaced by the shareable closet join
// link (token on the Group model: `joinToken` / `joinTokenExpiresAt`).
//
// This file is a tombstone because file deletion isn't available through the
// editing tools. Safe to delete manually: nothing imports it anymore.
export {};
