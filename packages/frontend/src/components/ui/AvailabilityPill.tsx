// Availability was removed from the app, so this component is no longer used.
// Left as an empty module because file deletion isn't available via tooling —
// safe to delete the file manually.
export {};
